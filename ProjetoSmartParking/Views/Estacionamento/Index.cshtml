@model IEnumerable<ProjetoSmartParking.Models.Estacionamento>

@{
    ViewBag.Title = "Index";
}

<h2>Smart Parking</h2>

<p>
    @Html.ActionLink("Cadastrar estacionamento", "Cadastrar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cnpj)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrecoHora)
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cnpj)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrecoHora)
        </td>
       
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.EstacionamentoId }) |
            @Html.ActionLink("Detalhes", "Detalhes", new { id=item.EstacionamentoId }) |
            @Html.ActionLink("Remover", "Remover", new { id=item.EstacionamentoId })
        </td>
    </tr>
}

</table>
